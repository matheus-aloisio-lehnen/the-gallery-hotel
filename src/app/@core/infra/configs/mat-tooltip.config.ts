import { MatTooltipDefaultOptions } from "@angular/material/tooltip";

export const MAT_TOOLTIP_CONFIG: Partial<MatTooltipDefaultOptions> = {
    position: "after",
    tooltipClass: 'tooltip-primary',
}
